{
  "name": "@proofhound/llm-client",
  "version": "0.1.7",
  "description": "LLM invocation wrapper — carries the docs/specs/05-logging.md §5.6 logging contract + upfront rate limiting",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit --incremental false",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "test:unit": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@proofhound/db": "workspace:*",
    "@proofhound/limiter": "workspace:*",
    "@proofhound/logger": "workspace:*",
    "@proofhound/shared": "workspace:*",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/proofhound/proofhound",
    "directory": "packages/llm-client"
  },
  "homepage": "https://github.com/proofhound/proofhound/tree/master/packages/llm-client#readme",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
