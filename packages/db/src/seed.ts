/**
 * Self-hosted seed placeholder.
 *
 * Open-source ProofHound does not create platform users or external Auth rows.
 * Use `pnpm db:seed:dev` for local fixture data.
 */
async function main(): Promise<void> {
  console.warn('ProofHound self-hosted seed: no platform users to create.');
}

void main();
