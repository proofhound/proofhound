name: ci

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Unit / lint / typecheck / circular-deps / terminology gate (mirrors `pnpm run ci`).
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run ci

  # Functional end-to-end suite against a real server/worker/webhook + ephemeral Postgres/Redis,
  # with the LLM faked (Playwright starts the fake server + the web dev server itself).
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: proofhound
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
      redis:
        image: redis:8
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      # Match the validated local dev behavior (LOCAL_ACTOR fallback, CORS origin, etc.).
      NODE_ENV: development
      LOG_LEVEL: info
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/proofhound
      REDIS_URL: redis://localhost:6379
      # CI-only throwaway secrets (NOT real). base64 of 32 bytes for the model-key encryption.
      MODEL_API_KEY_ENCRYPTION_KEY: MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
      MCP_TOKEN_SIGNING_SECRET: ci-mcp-signing-secret
      SERVER_PORT: "4000"
      WEB_PUBLIC_URL: http://localhost:3000
      NEXT_PUBLIC_SERVER_URL: http://localhost:4000
      WEBHOOK_BODY_LIMIT: 1mb
      WORKER_QUEUES: llm,probe
      WORKER_CONCURRENCY: "16"
      CANARY_RUNNER_SCAN_INTERVAL_MS: "2000"
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: Install Playwright Chromium
        run: pnpm --filter @proofhound/web exec playwright install --with-deps chromium

      - name: Apply database migrations
        run: pnpm db:migrate

      - name: Build backend services
        run: pnpm turbo run build --filter=@proofhound/server --filter=@proofhound/webhook --filter=@proofhound/worker

      - name: Start backend services
        run: |
          node apps/server/dist/main.js  > /tmp/server.log  2>&1 &
          node apps/webhook/dist/main.js > /tmp/webhook.log 2>&1 &
          node apps/worker/dist/main.js  > /tmp/worker.log  2>&1 &
          echo "Waiting for API server on :4000 ..."
          for i in $(seq 1 60); do
            if curl -sf -o /dev/null http://localhost:4000/datasets; then echo "server is up"; exit 0; fi
            sleep 2
          done
          echo "::error::API server did not become ready"; cat /tmp/server.log; exit 1

      - name: Run e2e suite
        run: pnpm --filter @proofhound/web test:e2e

      - name: Dump service logs on failure
        if: failure()
        run: |
          echo "===== server ====="; tail -n 200 /tmp/server.log  || true
          echo "===== webhook ====="; tail -n 200 /tmp/webhook.log || true
          echo "===== worker ====="; tail -n 200 /tmp/worker.log  || true

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            apps/web/playwright-report
            apps/web/test-results
          retention-days: 7
